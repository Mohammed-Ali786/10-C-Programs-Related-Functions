include <stdio.h>

int power(int base, int exp)
{
    int result = 1;
    for (int i = 1; i <= exp; i++)
    {
        result*=base;
    }
    return result;
}

int main()
{
    int base=2, exp=5;
    printf("%d^%d = %d",base,exp,power(base,exp));
    return 0;
}
