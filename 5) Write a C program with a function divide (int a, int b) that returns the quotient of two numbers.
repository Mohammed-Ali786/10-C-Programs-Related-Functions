 #include <stdio.h>

float divide(int a, int b)
{
    if (b==0)
    {
        printf("Error: Division by zero\n");
        return 0;
    }
    return (float)a/b;
}

int main()
{
    int x=10, y=4;
    printf("Quotient=%2f", divide(x,y));
    return 0;
}
